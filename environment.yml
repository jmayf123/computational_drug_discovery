name: drug_discovery
channels:
  - defaults
  - https://conda.anaconda.org/rdkit
  - conda-forge
dependencies:
  #  chembl_webresource_client --- you must pip install this package in your environment afterwards
  #  padelpy --- you must pip install in env
  - python
  - jupyter
  - numpy
  - pandas
  - seaborn
  - scikit-learn
  - rdkit::rdkit
